/*import CallSharkSDK from 'callshark-sdk'

const sdk = new CallSharkSDK({
  apiUrl: 'https://api.callshark.com',
  wsUrl: 'wss://ws.callshark.com',
  initialToken: 'your-initial-token'
})

// 1️⃣ Авторизация
sdk.authenticate()
  .then(() => {
    // 2️⃣ Подключаем WebSocket после получения нового токена
    sdk.connectSocket()

    // 3️⃣ Работа с API
    sdk.getCallInfo('12345').then(console.log)
    sdk.acceptCall('12345')
    sdk.endCall('12345')
  })
  .catch(console.error)*/
